version: "3.7"
services:
  nuxt-sample:
    build:
      context: .
      dockerfile: Dockerfile
    tty: true
    ports:
      - 3000:3000
    volumes:
      - ..:/app/:delegated
    container_name: nuxt-sample